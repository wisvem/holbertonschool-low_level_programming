#include <stdio.h>

/**
 * main - Entry point
 * print base 10 numbers
 * Return: Always 0 (Success)
**/
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d", num);
	puts('\n');
	return (0);
}
